
    <!-- Modal -->
  <div class="modal fade" id="submitMeetup" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Add your meetup</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <p>Form will go here...</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="button" class="btn btn-primary">Create</button>
        </div>
      </div>
    </div>
  </div>
<%= render "shared/airport_nav" %>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-9 mt-3 p-3">
      <h1><%=@airport.name %></h1>
        <% if @airport.terminal.present? %>
          <h2 class="fs-3"><strong>Terminal <%= @airport.terminal%></strong></h2>
          <h3 class="my-3">Meetups  🍿</h3>
        <% end %>
    </div>
  </div>
  <%= render "shared/my_flight", flights: @flights %>
  <div class="row">
    <div class="col">
      <div data-controller="search-meetups">
        <%= form_with url: airport_meetups_path,
          method: :get,
          html: {class: "mb-4", data: {search_meetups_target: "form"}} do |f|
        %>
          <%= f.text_field :query,
            class: "form-control",
            placeholder: "Type a meetups content",
            value: params[:query],
            data: {
              search_meetups_target: "input",
              action: "keyup->search-meetups#update"
            }
          %>
        <% end %>
        <%= render "filter", meetup_category: @meetup_category%>
        <%= render "list", meetups: @meetups, mymeetup: @mymeetup %>

      </div>
    </div>
  </div>
</div>
